# profiles/ — 役割別プロファイル（未実装）

対象・役割ごとの stack プリセット（例: `backend-lead.yaml` / `mlops-pm.yaml` / `cloud-native-consultant.yaml` / `small-poc.yaml`）を置く予定の場所。

まだ実体はない。生メモにはファイル名の案のみでスキーマの根拠がないため、項目設計（[`../framework-stack.yaml`](../framework-stack.yaml) の形式を対象別に切り出すか等）は owner 判断で確定してから作る。それまでの対象別の濃淡は [`../tailoring-policy.md`](../tailoring-policy.md) の表を正本とする。
