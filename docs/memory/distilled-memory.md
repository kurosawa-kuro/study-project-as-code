# Distilled Memory

`docs/decisions/decision-log.md` から `distill-memory` で蒸留した再利用可能な判断原則。抽象・少数に保つ — runtime で読む唯一の memory 層。判断が再発したか高損失だった後にだけ追加する（`docs/specs/judgment-memory.md`）。

<!-- 形式:
## <slug> — 一行原則
- when: 適用される状況
- do: 下すべき判断
- why: それを得た再発 / 損失（decision-log エントリをリンク）
-->

_まだ蒸留済み原則はありません。decision-log が証拠を蓄積するにつれ `distill-memory` が追加します。_
